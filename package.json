{
  "name": "@noirbright/dsh-plugins-directory",
  "version": "1.1.3",
  "private": true,
  "description": "Machine-readable catalog of DSH plugins and mobile companion published by NOirBRight",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "check": "node scripts/validate.mjs && node scripts/generate.mjs --check",
    "test": "node --test tests/*.test.mjs && npm run check",
    "verify:remote": "node scripts/verify-remote.mjs"
  }
}
